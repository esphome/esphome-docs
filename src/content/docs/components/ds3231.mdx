---
title: "DS3231 Real-Time Clock"
description: "Instructions for setting up the DS3231 real-time clock in ESPHome."
---

import { Image } from 'astro:assets';
import APIRef from '@components/APIRef.astro';

The `ds3231` component provides support for the Maxim/Analog Devices DS3231 real-time clock (RTC). The DS3231
is a temperature-compensated RTC accurate to a few parts per million, and is found on inexpensive breakout
boards such as the ZS-042. In addition to keeping time it provides two programmable alarms, a temperature
sensor, a configurable square-wave output and a trim (aging offset) register.

<Image src="/images/ds3231.png" width={300} height={300} layout="constrained" alt="DS3231 RTC breakout board" />

This component is an I²C hub. It talks to the chip and exposes the individual features through separate
platforms:

| Feature | Description |
| ------- | ----------- |
| [`time`](/components/time/ds3231/) | use the RTC as a time source |
| [`sensor`](/components/sensor/ds3231/) | read the on-die temperature |
| [`binary_sensor`](/components/binary_sensor/ds3231/) | alarm-fired and oscillator-stopped flags |
| [`switch`](/components/switch/ds3231/) | enable the alarms, enable the 32 kHz output |
| [`select`](/components/select/ds3231/) | set the `INT/SQW` pin mode and square-wave frequency |
| [`number`](/components/number/ds3231/) | adjust the aging offset |

The [I²C](/components/i2c/) bus must be set up first.

```yaml
# Example configuration entry
i2c:
  sda: GPIOXX
  scl: GPIOXX

ds3231:
```

## Configuration Variables

- **id** (*Optional*, [ID](/guides/configuration-types#id)): The ID to use for referencing this hub from
  the platforms and actions listed below.
- **address** (*Optional*, int): The I²C address of the chip. Defaults to `0x68`.
- **square_wave_output** (*Optional*, enum): The square-wave frequency for the `INT/SQW` pin. One of `1Hz`,
  `1.024kHz`, `4.096kHz` or `8.192kHz`. Used when the pin starts in square-wave mode and by the runtime
  [frequency select](/components/select/ds3231/). It does not by itself route the pin, that is
  `int_sqw_startup_mode`, which defaults to `square_wave` when this is set. See
  [Square-Wave Output](#square-wave-output).
- **int_sqw_startup_mode** (*Optional*, enum): How the `INT/SQW` pin comes up: `alarm_interrupt` (the
  alarm-interrupt line) or `square_wave` (emits `square_wave_output`). Defaults to `square_wave` when
  `square_wave_output` is set, otherwise `alarm_interrupt`. Change it at runtime with the
  [`select`](/components/select/ds3231/) platform.
- **battery_backed_square_wave** (*Optional*, boolean): Keep the square wave running while the chip is on
  battery power. Requires `square_wave_output`. Defaults to `false`.
- **enable_32khz_output** (*Optional*, boolean): Set to `false` to turn the chip's dedicated `32K` output off
  at boot. Defaults to `true`, which leaves the chip's own power-on state alone (a cold power-on has the
  32 kHz output enabled). Independent of the [`switch`](/components/switch/ds3231/) platform's
  `enable_32khz_output` entity, which controls it at runtime; the two can be used together.
- **alarm_1** (*Optional*): Program a default alarm 1 into the chip at boot. See
  [Programming an Alarm at Boot](#programming-an-alarm-at-boot).
- **alarm_2** (*Optional*): As `alarm_1`, for alarm 2.
- **on_alarm_1** (*Optional*, [Automation](/automations)): Automation to run when alarm 1 fires. See
  [Alarms](#alarms).
- **on_alarm_2** (*Optional*, [Automation](/automations)): Automation to run when alarm 2 fires.
- **update_interval** (*Optional*, [Time](/guides/configuration-types#time)): How often to poll the alarm
  flags and the oscillator-stopped flag (and republish the switch/select state). Defaults to `30s`. This is
  the initial value; it can be changed at runtime with
  [`ds3231.set_refresh_interval`](#ds3231-set_refresh_interval-action) or the
  [`refresh_interval` number](/components/number/ds3231/).
- **i2c_id** (*Optional*, [ID](/guides/configuration-types#id)): The ID of the [I²C](/components/i2c/) bus
  when using multiple buses.

Multiple DS3231 chips (on different addresses or buses) can be configured by giving each one its own entry
with a distinct `id`.

## Day of Week

This component uses ESPHome's day-of-week convention everywhere, matching the
[time](/components/time/) component: `1` = Sunday, `2` = Monday, through `7` = Saturday. That is what the
[`time`](/components/time/ds3231/) platform reports and what the `day_of_week` field of the
[`ds3231.set_alarm_1`](#ds3231-set_alarm_1-action) / [`ds3231.set_alarm_2`](#ds3231-set_alarm_2-action)
actions expects.

## Square-Wave Output

The `INT/SQW` pin can act either as an alarm interrupt or as a square-wave generator, but not both at once.

- In **alarm-interrupt** mode the pin idles high and is pulled low by the chip while a programmed alarm's
  flag is set, then released when the flag is cleared.
- In **square-wave** mode the pin emits `square_wave_output` (`1Hz` by default). This is useful for clocking
  external hardware or for confirming the chip is alive with a scope or LED.

`int_sqw_startup_mode` picks which mode the pin boots in. It defaults to `square_wave` when
`square_wave_output` is set and `alarm_interrupt` otherwise, so a config that only sets `square_wave_output`
behaves as you would expect. Set `square_wave_output` together with `int_sqw_startup_mode: alarm_interrupt`
to boot as the alarm line but be ready to switch to that frequency later.

Programming an alarm with the [`ds3231.set_alarm_1`](#ds3231-set_alarm_1-action) or
[`ds3231.set_alarm_2`](#ds3231-set_alarm_2-action) action forces the pin back to alarm-interrupt mode.

The pin can also be controlled at runtime from a [lambda](/automations/templates#config-lambda):

- `set_square_wave_output_enabled(true)` drives the square wave (at the current frequency, defaulting to the
  `square_wave_output` value or 1 Hz); `false` returns the pin to alarm-interrupt mode.
  `get_square_wave_output_enabled()` reads the mode back from the chip.
- `set_square_wave_frequency(...)` changes the frequency; `get_square_wave_frequency()` reads it back.

```cpp
using esphome::ds3231::DS3231SquareWaveFrequency;
id(ds3231_hub)->set_square_wave_frequency(DS3231SquareWaveFrequency::DS3231_SQUARE_WAVE_FREQUENCY_8192_HZ);
id(ds3231_hub)->set_square_wave_output_enabled(true);
```

The frequency values are `DS3231_SQUARE_WAVE_FREQUENCY_1_HZ`, `_1024_HZ`, `_4096_HZ` and `_8192_HZ`.
Enabling an alarm still forces the pin back to interrupt mode.

The [`select`](/components/select/ds3231/) platform exposes both of these (pin mode and frequency) as entities
without writing a lambda.

## Alarms

The DS3231 has two independent alarms:

- **Alarm 1** has one-second resolution.
- **Alarm 2** has one-minute resolution and always fires at second `0`.

Alarms are usually programmed at runtime with the actions below, or a default can be written into the chip at
boot with the [`alarm_1` / `alarm_2` hub options](#programming-an-alarm-at-boot). Each alarm sets a "fired"
flag in the chip when it matches. This component polls those flags every `update_interval`, runs the matching
`on_alarm_1` / `on_alarm_2` automation, updates the
[alarm binary sensors](/components/binary_sensor/ds3231/) and then clears the flag so the next match can be
detected.

An alarm stays enabled after it fires. [`ds3231.clear_alarm`](#ds3231-clear_alarm-action) only clears the
fired flag, it does not disable the alarm. To disable an alarm while keeping its programmed time (so it can be
re-enabled later), use [`ds3231.disable_alarm`](#ds3231-disable_alarm-action) or turn off its
[`switch`](/components/switch/ds3231/).

The `on_alarm_1` / `on_alarm_2` triggers and the `alarm_1` / `alarm_2`
[binary sensors](/components/binary_sensor/ds3231/) only react while that alarm is enabled. Disabling an alarm
suppresses them without clearing its programmed time; a match that occurs while the alarm is disabled is
ignored, and re-enabling does not replay it.

> [!NOTE]
> Because the flags are polled, the automation runs up to `update_interval` after the alarm actually
> matched. Lower `update_interval` for tighter timing, or wire the `INT/SQW` pin to a
> [GPIO binary sensor](/components/binary_sensor/gpio/) to react immediately.

> [!NOTE]
> Since the component clears the alarm flags on every poll, the `INT/SQW` pin is only held low until the next
> poll (up to `update_interval`). That is enough to wake a sleeping ESP through that pin. If you need the
> hardware pulse held longer while the device is running, raise `update_interval`. The `on_alarm_1` /
> `on_alarm_2` automations fire either way.

### Programming an Alarm at Boot

The `alarm_1` and `alarm_2` blocks on the `ds3231:` hub write a default alarm into the chip during setup. The
runtime actions, the [`switch`](/components/switch/ds3231/) and any alarm builder can change it afterwards.

```yaml
ds3231:
  id: ds3231_hub
  alarm_1:
    mode: MATCH_HOUR_MINUTE_SECOND
    hour: 7
    minute: 30
  alarm_2:
    mode: MATCH_MINUTE
    minute: 0
    enabled: false
```

The keys are the same as the [`ds3231.set_alarm_1`](#ds3231-set_alarm_1-action) /
[`ds3231.set_alarm_2`](#ds3231-set_alarm_2-action) actions but are not templatable:

- **mode** (**Required**, enum): The match condition. See the action for the value list (`alarm_1` and
  `alarm_2` have different sets).
- **second** (*Optional*, int): `alarm_1` only. Second to match, `0`&ndash;`59`. Defaults to `0`.
- **minute** (*Optional*, int): Minute to match, `0`&ndash;`59`. Defaults to `0`.
- **hour** (*Optional*, int): Hour to match (24-hour), `0`&ndash;`23`. Defaults to `0`.
- **day_of_week** (*Optional*, int): Day of week, `1` (Sunday) to `7` (Saturday). Required for
  `mode: MATCH_DAY_OF_WEEK`.
- **day_of_month** (*Optional*, int): Day of month, `1`&ndash;`31`. Required for `mode: MATCH_DAY_OF_MONTH`.
- **enabled** (*Optional*, boolean): `true` arms the alarm and routes `INT/SQW` to the interrupt line (even
  when `int_sqw_startup_mode` is `square_wave`, consistent with arming an alarm at runtime). `false` programs
  the match time but leaves the alarm disarmed and the pin routing untouched. Defaults to `true`.

`day_of_week` and `day_of_month` are mutually exclusive.

<span id="ds3231-set_alarm_1-action"></span>

## `ds3231.set_alarm_1` Action

Program and enable alarm 1.

```yaml
on_...:
  - ds3231.set_alarm_1:
      id: ds3231_hub
      mode: MATCH_HOUR_MINUTE_SECOND
      hour: 7
      minute: 30
      second: 0
```

Configuration variables:

- **id** (**Required**, [ID](/guides/configuration-types#id)): The ID of the DS3231 hub.
- **mode** (**Required**, enum): Which fields must match for the alarm to fire:
  - `EVERY_SECOND`: fires once per second.
  - `MATCH_SECOND`: when `second` matches.
  - `MATCH_MINUTE_SECOND`: when `minute` and `second` match.
  - `MATCH_HOUR_MINUTE_SECOND`: when `hour`, `minute` and `second` match (once per day).
  - `MATCH_DAY_OF_MONTH`: additionally when `day_of_month` matches.
  - `MATCH_DAY_OF_WEEK`: additionally when `day_of_week` matches.
- **second** (*Optional*, int, [templatable](/automations/templates)): Second to match, `0` to
  `59`. Defaults to `0`.
- **minute** (*Optional*, int, [templatable](/automations/templates)): Minute to match, `0` to
  `59`. Defaults to `0`.
- **hour** (*Optional*, int, [templatable](/automations/templates)): Hour to match (24-hour),
  `0` to `23`. Defaults to `0`.
- **day_of_week** (*Optional*, int, [templatable](/automations/templates)): Day of week to
  match, `1` (Sunday) to `7` (Saturday). Only for `mode: MATCH_DAY_OF_WEEK`.
- **day_of_month** (*Optional*, int, [templatable](/automations/templates)): Day of month to
  match, `1` to `31`. Only for `mode: MATCH_DAY_OF_MONTH`.

`day_of_week` and `day_of_month` are mutually exclusive.

> [!NOTE]
> `day_of_week` follows ESPHome's convention (`1` = Sunday, see [Day of Week](#day-of-week)). A day-of-week
> alarm matches the RTC as long as the clock was set through the [`time`](/components/time/ds3231/) platform or
> the [`ds3231.write_time`](/components/time/ds3231/#ds3231-write_time-action) action.

<span id="ds3231-set_alarm_2-action"></span>

## `ds3231.set_alarm_2` Action

Program and enable alarm 2. Identical to `ds3231.set_alarm_1` except that there is no `second` field and the
available modes are `EVERY_MINUTE`, `MATCH_MINUTE`, `MATCH_HOUR_MINUTE`, `MATCH_DAY_OF_MONTH` and
`MATCH_DAY_OF_WEEK`.

```yaml
on_...:
  - ds3231.set_alarm_2:
      id: ds3231_hub
      mode: MATCH_DAY_OF_WEEK
      day_of_week: 2  # Monday
      hour: 8
      minute: 0
```

The alarm actions do different things:

| Action | Effect |
| ------ | ------ |
| `ds3231.set_alarm_1` / `_2` | Programs the match time **and** enables the alarm |
| `ds3231.enable_alarm` | Enables the alarm, keeping its programmed time |
| `ds3231.disable_alarm` | Disables the alarm, keeping its programmed time |
| `ds3231.clear_alarm` | Clears only the "fired" flag; the alarm stays enabled |

<span id="ds3231-clear_alarm-action"></span>

## `ds3231.clear_alarm` Action

Clear a pending alarm's "fired" flag, so it can be detected again on the next match. This does **not** disable
the alarm.

```yaml
on_...:
  - ds3231.clear_alarm:
      id: ds3231_hub
      alarm: 1
```

Configuration variables:

- **id** (**Required**, [ID](/guides/configuration-types#id)): The ID of the DS3231 hub.
- **alarm** (**Required**, int): Which alarm, `1` or `2`.

<span id="ds3231-enable_alarm-action"></span>

## `ds3231.enable_alarm` Action

Enable an alarm without reprogramming its match time. This is the action form of turning the alarm's
[`switch`](/components/switch/ds3231/) on.

```yaml
on_...:
  - ds3231.enable_alarm:
      id: ds3231_hub
      alarm: 1
```

Configuration variables:

- **id** (**Required**, [ID](/guides/configuration-types#id)): The ID of the DS3231 hub.
- **alarm** (**Required**, int): Which alarm, `1` or `2`.

> [!NOTE]
> Enabling an alarm whose registers were never programmed (with
> [`ds3231.set_alarm_1`](#ds3231-set_alarm_1-action) / [`ds3231.set_alarm_2`](#ds3231-set_alarm_2-action))
> enables whatever the registers happen to hold.

<span id="ds3231-disable_alarm-action"></span>

## `ds3231.disable_alarm` Action

**Disable an alarm**": the `on_alarm_` trigger and the alarm binary sensor stop reacting while
keeping its programmed match time. Re-enable it with [`ds3231.enable_alarm`](#ds3231-enable_alarm-action),
[`ds3231.set_alarm_1`](#ds3231-set_alarm_1-action) / [`ds3231.set_alarm_2`](#ds3231-set_alarm_2-action), or by
turning its [`switch`](/components/switch/ds3231/) on. This is the action form of turning the alarm switch off.

```yaml
on_...:
  - ds3231.disable_alarm:
      id: ds3231_hub
      alarm: 1
```

Configuration variables:

- **id** (**Required**, [ID](/guides/configuration-types#id)): The ID of the DS3231 hub.
- **alarm** (**Required**, int): Which alarm, `1` or `2`.

<span id="ds3231-force_temperature_conversion-action"></span>

## `ds3231.force_temperature_conversion` Action

Trigger an immediate temperature conversion instead of waiting for the chip's automatic 64-second cycle. The
result is also used to re-tune the oscillator.

```yaml
on_...:
  - ds3231.force_temperature_conversion: ds3231_hub
```

<span id="ds3231-set_refresh_interval-action"></span>

## `ds3231.set_refresh_interval` Action

Change how often the hub polls the chip at runtime (see
[`update_interval`](#configuration-variables)). The [`refresh_interval` number](/components/number/ds3231/)
does the same thing as an entity.

```yaml
on_...:
  - ds3231.set_refresh_interval:
      id: ds3231_hub
      refresh_interval: 30s
```

Configuration variables:

- **id** (**Required**, [ID](/guides/configuration-types#id)): The ID of the DS3231 hub.
- **refresh_interval** (**Required**, [Time](/guides/configuration-types#time),
  [templatable](/automations/templates)): The new poll interval.

## Reading the Programmed Alarm

The hub exposes the alarm currently programmed into the chip so you can display it, for example in a
[template text sensor](/components/text_sensor/template/):

```yaml
text_sensor:
  - platform: template
    name: DS3231 Alarm 1
    lambda: |-
      char buf[40];
      if (!id(ds3231_hub)->describe_alarm_1(buf, sizeof(buf)))
        return {"not set"};
      return {buf};
```

`describe_alarm_1()` / `describe_alarm_2()` write a short summary into `buf` and return `false` (leaving `buf`
untouched) when the alarm registers can't be decoded, for example on a fresh chip whose alarm registers
were never written. The summaries read like:

- Alarm 1: `every second`, `every minute at :SS`, `every hour at MM:SS`, `daily at HH:MM:SS`,
  `day D at HH:MM:SS`, `every Wed at HH:MM:SS`
- Alarm 2: `every minute`, `every hour at MM:00`, `daily at HH:MM`, `day D at HH:MM`, `every Wed at HH:MM`

Day-of-week alarms use the abbreviated weekday name (`Sun`&ndash;`Sat`, from `strftime`), or `day N` if the
register holds an out-of-range value. `day D` is a calendar day of the month. The programmed alarms are also
printed at startup in the component's `dump_config` output.

For structured access, `get_alarm_1(mode, spec)` / `get_alarm_2(mode, spec)` read the mode and the field
values (`DS3231AlarmSpec` has `second`, `minute`, `hour` and `day`) back from the chip. They return `false` on
a bus error or an unrecognized register pattern.

## Hardware Notes

- On common breakout boards (ZS-042 and similar) a separate AT24C32 EEPROM is fitted at address `0x57`. It is
  not used by this component but will show up in an [I²C scan](/components/i2c/).
- Setting an alarm repurposes the `INT/SQW` pin, so a configured square-wave output stops once an alarm is
  programmed.
- If the chip reports that its oscillator stopped (loss of both `VCC` and battery), the stored time is not
  trusted: the [`time`](/components/time/ds3231/) platform refuses to sync from it until a valid time is
  written back with [`ds3231.write_time`](/components/time/ds3231/#ds3231-write_time-action).

## Minimal Example

The core of a DS3231 setup: the hub, the RTC as a [time source](/components/time/ds3231/), and the on-die
[temperature sensor](/components/sensor/ds3231/). The RTC is written from a network time source whenever that
source synchronizes; see [`ds3231.write_time`](/components/time/ds3231/#ds3231-write_time-action).

```yaml
i2c:
  sda: GPIOXX
  scl: GPIOXX

ds3231:
  id: ds3231_hub

time:
  - platform: ds3231
    ds3231_id: ds3231_hub
    id: ds3231_time
  - platform: sntp
    on_time_sync:
      then:
        - ds3231.write_time: ds3231_time

sensor:
  - platform: ds3231
    ds3231_id: ds3231_hub
    name: "RTC Temperature"
```

<span id="ds3231-config_example"></span>

## Full Configuration Example

This example exercises every platform and action. `INT/SQW` is left as the alarm-interrupt line (no
`square_wave_output`), with a [GPIO binary sensor](/components/binary_sensor/gpio/) on it as the immediate
hardware path and the [`select`](/components/select/ds3231/) able to switch it to a square wave at runtime.
The [web server](/components/web_server/) `sorting_groups` keep the DS3231 entities together on the dashboard.

The alarms are set two ways, side by side. The *Apply (via action)* buttons use the declarative
[`ds3231.set_alarm_1`](#ds3231-set_alarm_1-action) / [`ds3231.set_alarm_2`](#ds3231-set_alarm_2-action)
actions; their `mode` must be a fixed literal, so the templated time fields come from the builder numbers but
the mode is fixed. The *Apply (via lambda)* buttons call `id(ds3231_hub)->set_alarm_1(mode, spec)` directly,
mapping the *Mode* [select](/components/select/template/) at runtime so every mode is reachable.

```yaml
# Version 3 is required for sorting_groups.
web_server:
  version: 3
  port: 80
  sorting_groups:
    - id: ds3231_group
      name: DS3231
      sorting_weight: 10
    - id: ds3231_alarm_group
      name: DS3231 Alarms
      sorting_weight: 20
    # Input fields for composing an alarm. Nothing here touches the chip until
    # the matching "Apply" button is pressed.
    - id: ds3231_alarm1_builder_group
      name: DS3231 Alarm 1 Builder
      sorting_weight: 30
    - id: ds3231_alarm2_builder_group
      name: DS3231 Alarm 2 Builder
      sorting_weight: 40
    - id: ds3231_actions_group
      name: DS3231 Actions
      sorting_weight: 50

i2c:
  - id: i2c_bus
    sda: GPIOXX
    scl: GPIOXX
    scan: true
    frequency: 400kHz

# No square_wave_output is set, so int_sqw_startup_mode defaults to alarm_interrupt:
# INT/SQW sits high and is pulled low by the chip whenever a programmed alarm matches.
ds3231:
  - id: ds3231_hub
    i2c_id: i2c_bus
    address: 0x68
    # How often the alarm flags / oscillator-stop flag are polled.
    update_interval: 10s
    # Program a default alarm 1 at boot, left disarmed. The runtime buttons /
    # switch / builder below can change or enable it.
    alarm_1:
      mode: MATCH_HOUR_MINUTE_SECOND
      hour: 7
      minute: 30
      enabled: false
    # Software path: fires from the polled alarm flags (the INT/SQW binary_sensor
    # below is the hardware path and reacts immediately).
    on_alarm_1:
      then:
        - logger.log: "DS3231 alarm 1 fired"
    on_alarm_2:
      then:
        - logger.log: "DS3231 alarm 2 fired"

# Use SNTP to get real time, then push it into the DS3231 on every sync.
time:
  - platform: sntp
    id: sntp_time
    on_time_sync:
      then:
        - ds3231.write_time: ds3231_time
  - platform: ds3231
    ds3231_id: ds3231_hub
    id: ds3231_time
    # Pull time from the RTC into the system clock every minute.
    update_interval: 1min

sensor:
  - platform: ds3231
    ds3231_id: ds3231_hub
    name: DS3231 Temperature
    # celsius (default) or fahrenheit; also sets unit_of_measurement to match.
    unit: celsius
    update_interval: 30s
    web_server:
      sorting_group_id: ds3231_group

text_sensor:
  # The system clock, kept in sync by the ds3231 time platform.
  - platform: template
    name: DS3231 RTC Time
    update_interval: 5s
    web_server:
      sorting_group_id: ds3231_group
    lambda: |-
      auto t = id(ds3231_time).now();
      if (!t.is_valid()) return {"unknown"};
      return t.strftime("%Y-%m-%d %H:%M:%S");
  # The alarms actually programmed into the chip's registers.
  - platform: template
    name: DS3231 Alarm 1 Setting
    update_interval: 10s
    web_server:
      sorting_group_id: ds3231_alarm_group
    lambda: |-
      char buf[40];
      if (!id(ds3231_hub)->describe_alarm_1(buf, sizeof(buf))) return {"not set"};
      return {buf};
  - platform: template
    name: DS3231 Alarm 2 Setting
    update_interval: 10s
    web_server:
      sorting_group_id: ds3231_alarm_group
    lambda: |-
      char buf[40];
      if (!id(ds3231_hub)->describe_alarm_2(buf, sizeof(buf))) return {"not set"};
      return {buf};

binary_sensor:
  - platform: ds3231
    ds3231_id: ds3231_hub
    # "Fired" flags (A1F/A2F): on when the alarm has matched, cleared on the next poll.
    alarm_1:
      name: DS3231 Alarm 1 Fired
      web_server:
        sorting_group_id: ds3231_alarm_group
    alarm_2:
      name: DS3231 Alarm 2 Fired
      web_server:
        sorting_group_id: ds3231_alarm_group
    oscillator_stopped:
      name: DS3231 Oscillator Stopped
      web_server:
        sorting_group_id: ds3231_group

  # Hardware path: the DS3231 pulls INT/SQW low the instant an alarm matches, so
  # this reacts without waiting for the next poll. The line is open-drain, so use
  # the internal pull-up and invert (low = active).
  - platform: gpio
    name: DS3231 Alarm Line (INT/SQW)
    pin:
      number: GPIOXX
      mode:
        input: true
        pullup: true
      inverted: true
    web_server:
      sorting_group_id: ds3231_alarm_group

switch:
  - platform: ds3231
    ds3231_id: ds3231_hub
    enable_32khz_output:
      name: DS3231 32 kHz Output
      web_server:
        sorting_group_id: ds3231_group
    # Enable/disable an alarm without reprogramming its time.
    alarm_1:
      name: DS3231 Alarm 1 Enabled
      web_server:
        sorting_group_id: ds3231_alarm_group
    alarm_2:
      name: DS3231 Alarm 2 Enabled
      web_server:
        sorting_group_id: ds3231_alarm_group

number:
  - platform: ds3231
    ds3231_id: ds3231_hub
    aging_offset:
      name: DS3231 Aging Offset
      web_server:
        sorting_group_id: ds3231_group
    # Hub poll interval, settable at runtime (same knob as ds3231.set_refresh_interval).
    refresh_interval:
      name: DS3231 Refresh Interval
      web_server:
        sorting_group_id: ds3231_group

  # Alarm builder: input-only template fields. Day is day-of-month (1-31) or
  # weekday (1-7, 1 = Sunday) depending on the mode; ignored by the other modes.
  - platform: template
    id: alarm1_hour
    name: Alarm 1 Hour
    optimistic: true
    restore_value: true
    initial_value: 7
    min_value: 0
    max_value: 23
    step: 1
    mode: box
    web_server:
      sorting_group_id: ds3231_alarm1_builder_group
  - platform: template
    id: alarm1_minute
    name: Alarm 1 Minute
    optimistic: true
    restore_value: true
    initial_value: 30
    min_value: 0
    max_value: 59
    step: 1
    mode: box
    web_server:
      sorting_group_id: ds3231_alarm1_builder_group
  - platform: template
    id: alarm1_second
    name: Alarm 1 Second
    optimistic: true
    restore_value: true
    initial_value: 0
    min_value: 0
    max_value: 59
    step: 1
    mode: box
    web_server:
      sorting_group_id: ds3231_alarm1_builder_group
  - platform: template
    id: alarm1_day
    name: Alarm 1 Day
    optimistic: true
    restore_value: true
    initial_value: 1
    min_value: 1
    max_value: 31
    step: 1
    mode: box
    web_server:
      sorting_group_id: ds3231_alarm1_builder_group
  # Alarm 2 has no seconds field (it always matches at :00).
  - platform: template
    id: alarm2_hour
    name: Alarm 2 Hour
    optimistic: true
    restore_value: true
    initial_value: 8
    min_value: 0
    max_value: 23
    step: 1
    mode: box
    web_server:
      sorting_group_id: ds3231_alarm2_builder_group
  - platform: template
    id: alarm2_minute
    name: Alarm 2 Minute
    optimistic: true
    restore_value: true
    initial_value: 0
    min_value: 0
    max_value: 59
    step: 1
    mode: box
    web_server:
      sorting_group_id: ds3231_alarm2_builder_group
  - platform: template
    id: alarm2_day
    name: Alarm 2 Day
    optimistic: true
    restore_value: true
    initial_value: 2
    min_value: 1
    max_value: 31
    step: 1
    mode: box
    web_server:
      sorting_group_id: ds3231_alarm2_builder_group

select:
  - platform: ds3231
    ds3231_id: ds3231_hub
    output_mode:
      name: DS3231 INT/SQW Mode
      web_server:
        sorting_group_id: ds3231_group
    square_wave_frequency:
      name: DS3231 Square Wave Frequency
      web_server:
        sorting_group_id: ds3231_group

  # Alarm match mode (input-only, see the builder groups above).
  - platform: template
    id: alarm1_mode
    name: Alarm 1 Mode
    optimistic: true
    restore_value: true
    initial_option: Daily at H:M:S
    options:
      - Every second
      - Match seconds
      - Match minutes + seconds
      - Daily at H:M:S
      - Day-of-month at H:M:S
      - Weekday at H:M:S
    web_server:
      sorting_group_id: ds3231_alarm1_builder_group
  # Alarm 2 has no per-second modes.
  - platform: template
    id: alarm2_mode
    name: Alarm 2 Mode
    optimistic: true
    restore_value: true
    initial_option: Daily at H:M
    options:
      - Every minute
      - Match minutes
      - Daily at H:M
      - Day-of-month at H:M
      - Weekday at H:M
    web_server:
      sorting_group_id: ds3231_alarm2_builder_group

button:
  - platform: template
    name: DS3231 Sync System Time To RTC
    web_server:
      sorting_group_id: ds3231_actions_group
    on_press:
      - ds3231.write_time: ds3231_time

  - platform: template
    name: DS3231 Read RTC To System Time
    web_server:
      sorting_group_id: ds3231_actions_group
    on_press:
      - ds3231.read_time: ds3231_time

  - platform: template
    name: DS3231 Force Temperature Conversion
    web_server:
      sorting_group_id: ds3231_actions_group
    on_press:
      - ds3231.force_temperature_conversion: ds3231_hub

  - platform: template
    name: DS3231 Set Refresh Interval To 5s
    web_server:
      sorting_group_id: ds3231_actions_group
    on_press:
      - ds3231.set_refresh_interval:
          id: ds3231_hub
          refresh_interval: 5s

  # (1) Via a lambda calling the hub directly - handles every mode because it maps
  #     the "Alarm 1 Mode" select at runtime (set_alarm_1's mode is not templatable).
  - platform: template
    name: DS3231 Alarm 1 Apply (via lambda)
    web_server:
      sorting_group_id: ds3231_alarm1_builder_group
    on_press:
      - lambda: |-
          using namespace esphome::ds3231;
          DS3231AlarmSpec spec;
          spec.hour = (uint8_t) id(alarm1_hour).state;
          spec.minute = (uint8_t) id(alarm1_minute).state;
          spec.second = (uint8_t) id(alarm1_second).state;
          spec.day = (uint8_t) id(alarm1_day).state;
          auto sel = id(alarm1_mode).current_option();
          DS3231Alarm1Mode mode = DS3231Alarm1Mode::DS3231_ALARM_1_MODE_EVERY_SECOND;
          if (sel == "Match seconds") {
            mode = DS3231Alarm1Mode::DS3231_ALARM_1_MODE_MATCH_SECOND;
          } else if (sel == "Match minutes + seconds") {
            mode = DS3231Alarm1Mode::DS3231_ALARM_1_MODE_MATCH_MINUTE_SECOND;
          } else if (sel == "Daily at H:M:S") {
            mode = DS3231Alarm1Mode::DS3231_ALARM_1_MODE_MATCH_HOUR_MINUTE_SECOND;
          } else if (sel == "Day-of-month at H:M:S") {
            mode = DS3231Alarm1Mode::DS3231_ALARM_1_MODE_MATCH_DAY_OF_MONTH;
          } else if (sel == "Weekday at H:M:S") {
            mode = DS3231Alarm1Mode::DS3231_ALARM_1_MODE_MATCH_DAY_OF_WEEK;
          }
          id(ds3231_hub)->set_alarm_1(mode, spec);

  # (2) Via the ds3231.set_alarm_1 action. `mode` must be a fixed literal, so this
  #     button always programs "daily at H:M:S"; the time fields are templated from
  #     the same builder inputs.
  - platform: template
    name: DS3231 Alarm 1 Apply (via action)
    web_server:
      sorting_group_id: ds3231_alarm1_builder_group
    on_press:
      - ds3231.set_alarm_1:
          id: ds3231_hub
          mode: MATCH_HOUR_MINUTE_SECOND
          hour: !lambda "return (uint8_t) id(alarm1_hour).state;"
          minute: !lambda "return (uint8_t) id(alarm1_minute).state;"
          second: !lambda "return (uint8_t) id(alarm1_second).state;"

  # Same two-way comparison for alarm 2 (no seconds - the chip matches at :00).
  - platform: template
    name: DS3231 Alarm 2 Apply (via lambda)
    web_server:
      sorting_group_id: ds3231_alarm2_builder_group
    on_press:
      - lambda: |-
          using namespace esphome::ds3231;
          DS3231AlarmSpec spec;
          spec.hour = (uint8_t) id(alarm2_hour).state;
          spec.minute = (uint8_t) id(alarm2_minute).state;
          spec.day = (uint8_t) id(alarm2_day).state;
          auto sel = id(alarm2_mode).current_option();
          DS3231Alarm2Mode mode = DS3231Alarm2Mode::DS3231_ALARM_2_MODE_EVERY_MINUTE;
          if (sel == "Match minutes") {
            mode = DS3231Alarm2Mode::DS3231_ALARM_2_MODE_MATCH_MINUTE;
          } else if (sel == "Daily at H:M") {
            mode = DS3231Alarm2Mode::DS3231_ALARM_2_MODE_MATCH_HOUR_MINUTE;
          } else if (sel == "Day-of-month at H:M") {
            mode = DS3231Alarm2Mode::DS3231_ALARM_2_MODE_MATCH_DAY_OF_MONTH;
          } else if (sel == "Weekday at H:M") {
            mode = DS3231Alarm2Mode::DS3231_ALARM_2_MODE_MATCH_DAY_OF_WEEK;
          }
          id(ds3231_hub)->set_alarm_2(mode, spec);

  # (2) Via the ds3231.set_alarm_2 action - fixed "daily at H:M" mode, templated time.
  - platform: template
    name: DS3231 Alarm 2 Apply (via action)
    web_server:
      sorting_group_id: ds3231_alarm2_builder_group
    on_press:
      - ds3231.set_alarm_2:
          id: ds3231_hub
          mode: MATCH_HOUR_MINUTE
          hour: !lambda "return (uint8_t) id(alarm2_hour).state;"
          minute: !lambda "return (uint8_t) id(alarm2_minute).state;"

  - platform: template
    name: DS3231 Clear Alarm 1
    web_server:
      sorting_group_id: ds3231_actions_group
    on_press:
      - ds3231.clear_alarm:
          id: ds3231_hub
          alarm: 1

  - platform: template
    name: DS3231 Clear Alarm 2
    web_server:
      sorting_group_id: ds3231_actions_group
    on_press:
      - ds3231.clear_alarm:
          id: ds3231_hub
          alarm: 2

  # enable/disable set/clear A1IE/A2IE, keeping the programmed time - the action
  # form of the "Alarm N Enabled" switch.
  - platform: template
    name: DS3231 Enable Alarm 1
    web_server:
      sorting_group_id: ds3231_actions_group
    on_press:
      - ds3231.enable_alarm:
          id: ds3231_hub
          alarm: 1

  - platform: template
    name: DS3231 Disable Alarm 1
    web_server:
      sorting_group_id: ds3231_actions_group
    on_press:
      - ds3231.disable_alarm:
          id: ds3231_hub
          alarm: 1

  - platform: template
    name: DS3231 Enable Alarm 2
    web_server:
      sorting_group_id: ds3231_actions_group
    on_press:
      - ds3231.enable_alarm:
          id: ds3231_hub
          alarm: 2

  - platform: template
    name: DS3231 Disable Alarm 2
    web_server:
      sorting_group_id: ds3231_actions_group
    on_press:
      - ds3231.disable_alarm:
          id: ds3231_hub
          alarm: 2
```

## See Also

- [I²C Bus](/components/i2c/)
- [DS3231 Time Source](/components/time/ds3231/)
- [DS3231 Temperature Sensor](/components/sensor/ds3231/)
- <APIRef text="ds3231/ds3231.h" path="ds3231/ds3231.h" />
- [Time Component](/components/time/)
