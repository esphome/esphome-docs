---
description: "Instructions for setting up Zephyr hardware PWMs."
title: "Zephyr PWM Output"
---
import APIRef from '@components/APIRef.astro';


The Zephyr PWM platform allows you to use a hardware PWM on chips running the Zephyr RTOS like nRF52.

```yaml
# Example configuration entry
output:
  - platform: zephyr_pwm
    pin: P0.02
    frequency: 1kHz
    id: pwm_output

# Example usage in a light
light:
  - platform: monochromatic
    output: pwm_output
    name: "Kitchen Light"
```

## Configuration variables

- **pin** (**Required**, [Pin Schema](/guides/configuration-types#pin-schema)): The pin to use PWM on.
  `allow_other_uses` is not supported.
- **id** (**Required**, [ID](/guides/configuration-types#id)): The id to use for this output component.
- **frequency** (*Optional*, frequency): The frequency to run the PWM with. Defaults to `1kHz`. Must be
  between `3.815Hz` and `10MHz`.

- All other options from [Output](/components/output#config-output).

## Hardware Limits

The nRF52840 has 4 PWM blocks of 4 channels each. All channels within a block must share the same
**frequency** — ESPHome packs `zephyr_pwm` outputs into blocks by frequency automatically, but raises a
config error if more than 4 distinct frequencies are configured across all `zephyr_pwm` outputs, or more
than 4 outputs share the same frequency.

## See Also

- [nRF52 Platform](/components/nrf52/)
- [Output Component](/components/output/)
- [Zephyr Platform](/components/zephyr/)
- [Monochromatic Light](/components/light/monochromatic/)
- [Speed Fan](/components/fan/speed/)
- [Power Supply Component](/components/power_supply/)
- <APIRef text="zephyr_pwm.h" path="zephyr_pwm/zephyr_pwm.h" />
